# TODO

- Custom fluxer's use of vantage_set_state and lifx_set_state are not working reliably to change color when lights are off.
- Make follow [async style](https://developers.home-assistant.io/docs/en/asyncio_working_with_async.html).
- Switch to using proper yaml for exclude_name_substring, exclude_areas, only_areas.
- Document priority of exclude_areas vs. only_areas.
- Add only_name_substring (or include_name_string) to exclude everything else by default.
- Switch to an Integration Configuration [style](https://developers.home-assistant.io/docs/en/config_entries_config_flow_handler.html) instead of all in configuration.yaml.
